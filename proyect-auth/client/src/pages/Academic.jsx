import React from 'react'
import AcademicView from '../components/AcademicView'

const Academic = ({ grades, attendance, schedule }) => (
  <AcademicView grades={grades} attendance={attendance} schedule={schedule} />
)

export default Academic
